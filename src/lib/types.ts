import { z } from "zod";

export type SevenSElements = {
  strategy: string;
  structure: string;
  systems: string;
  sharedValues: string;
  style: string;
  staff: string;
  skills: string;
};

export type SwotElements = {
  strengths: string;
  weaknesses: string;
  opportunities: string;
  threats: string;
};
